name: Frontrunner production code

# Tests deliberately construct hostile regexes, shell arguments and malformed
# paths. Scan every reachable backend and UI source while keeping that one
# non-product tree from masking new production findings.
paths-ignore:
  - tests/**
